export function passwordVerifier(email,password) {
  if (!password) return "Oh no, wrong password... try again"
  // if (password.length < 5) return 'Password must be at least 5 characters long.'
  // if (getPassword(email) != password) return "Oh no, wrong password... try again"
  return ''
}
